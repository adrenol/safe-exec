# Direct dependencies for the yt-dlp container.
# Edit versions here, then regenerate the pinned requirements.txt with:
#   docker run --rm -v "$PWD:/w" -w /w alpine:3.22 sh -c \
#     'apk add --no-cache python3 py3-pip >/dev/null && pip install --break-system-packages -q pip-tools && \
#      pip-compile --generate-hashes --quiet --output-file requirements.txt requirements.in'
yt-dlp[default]==2026.06.09
bgutil-ytdlp-pot-provider==1.3.1
curl_cffi==0.15.0
